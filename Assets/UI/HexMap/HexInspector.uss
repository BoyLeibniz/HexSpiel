.ui-root {
    width: 100%;
    height: 100%;
    position: relative;
}

.inspector-container {
    width: 200px;
    height: 100%;
    position: absolute;
    left: 0;
    top: 0;

    padding: 10px;
    background-color: #2D2D2D;
    color: #E0E0E0 ;
    display: flex;  
    flex-direction: column;
}

.inspector-container > * {
    margin-bottom: 6px;
}

.header {
    font-size: 16px;
    margin-bottom: 8px;
}

.value-label {
    color: #CCCCCC;
    font-size: 14px;
}

.grid-settings {
    margin-bottom: 12px;
}

.grid-settings > * {
    margin-bottom: 6px;
}

.map-name-container {
    flex-direction: row;
    height: 40px;
    margin-bottom: 8px;
    overflow: visible;
}

.combo-box-field {
    flex-direction: column;
    width: 100%;
    height: 100%;
    position: relative;
    overflow: visible;
}

.combo-box-row {
    display: flex;
    flex-direction: row;
    align-items: center;
    width: 100%;
    height: 100%;
    gap: 0;
    overflow: visible;
}

.combo-box-input {
    flex-grow: 1;
    height: 100%;
    min-width: 0; /* Allows proper shrink inside flexbox */
    max-width: 100%; /* Ensures it doesn't overflow */
    margin: 0;
    -unity-hide-clear-button: true;
}

.combo-box-button {
    width: 24px;
    min-width: 24px;
    height: 100%;
    padding: 0;
}

.combo-box-popup {
    position: absolute;
    top: 100%;     /* Pushes it directly below the row */
    z-index: 9999; /* Ensures it appears above other elements */
    left: 0;
    right: 0;

    background-color: white;
    border: 1px solid #999;
    padding: 2px;

    max-height: 200px;
    overflow-y: auto;
}

.combo-box-popup Label {
    color: black;
    background-color: white;
    padding: 4px;
    font-size: 13px;
}

.combo-box-popup Label:hover {
    background-color: #DDD;
    color: black;
    cursor: pointer;
}

.field-row {
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: flex-start;
    width: 100%;
    gap: 6px;
}

.field-label {
    width: 40px;
    color: #E0E0E0;
    font-size: 14px;
    flex-shrink: 0;
}

.divider {
    height: 8px;
}

.hex-field {
    flex-grow: 1;
    min-width: 0;   /* 🔑 Allows proper shrink inside flexbox */
    width: 71%; /* Ensures it doesn't overflow */
}

TextField {
    color: #2D2D2D; /* input field font color */
    min-width: 30;
}

IntegerField {
    color: #2D2D2D; /* input field font color */
    min-width: 0;
    max-width: 75%; 
}

Button {
    color: #FFFFFF;
    font-size: 15px;
    font-weight: bold;
}

#typeDropdown {
    color: white;
    margin-left: 0;
}

